# 🖥️ NF_Void Terminal

<div align="center">

[![Electron](https://img.shields.io/badge/Electron-28.0.0-47848F?style=for-the-badge&logo=electron&logoColor=white)](https://www.electronjs.org/)
[![Node.js](https://img.shields.io/badge/Node.js-20.x-339933?style=for-the-badge&logo=node.js&logoColor=white)](https://nodejs.org/)
[![License](https://img.shields.io/badge/License-MIT-green?style=for-the-badge)](LICENSE)
[![Platform](https://img.shields.io/badge/Platform-Windows%20%7C%20macOS%20%7C%20Linux-lightgrey?style=for-the-badge)](https://github.com/)

**Профессиональный десктопный терминал с инструментами кибербезопасности**

</div>

---

## 📖 Описание

**NF_Void Terminal** — это полнофункциональный десктопный терминал, созданный на базе **Electron.js**. Приложение предоставляет реальный доступ к файловой системе вашего компьютера, сетевым инструментам и функциям безопасности, всё в стильном интерфейсе в стиле классического терминала.

Идеально подходит для:
- 🔹 Системных администраторов
- 🔹 Специалистов по кибербезопасности
- 🔹 Разработчиков
- 🔹 Любителей командной строки

---

## ✨ Возможности

### 📁 Работа с файлами
| Функция | Описание |
|---------|----------|
| `ls`, `cd`, `pwd` | Навигация по директориям |
| `cat`, `touch`, `mkdir` | Чтение и создание файлов |
| `rm`, `cp`, `mv` | Управление файлами |
| `echo` | Вывод текста |

### 🌐 Сетевые инструменты
| Функция | Описание |
|---------|----------|
| `port` | Проверка статуса порта |
| `nmap` / `scan` | Сканирование портов (1-1024) |
| `netinfo` | Информация о сетевых интерфейсах |
| `dns` | DNS запросы |
| `traceroute` | Трассировка маршрута |

### 🛡️ Кибербезопасность
| Функция | Описание |
|---------|----------|
| `hash` | Генерация хешей (MD5, SHA1, SHA256, SHA512) |
| `passwd` | Генерация безопасных паролей |
| `secscan` / `audit` | Аудит файлов на уязвимости |
| `base64` | Кодирование/декодирование |
| `verify` | Проверка целостности файлов |
| `logs` | Просмотр логов безопасности |

### 💻 Системные команды
| Функция | Описание |
|---------|----------|
| `ps` / `processes` | Список процессов |
| `kill` | Завершение процессов |
| `sysinfo` | Информация о системе |
| `top` | Мониторинг ресурсов |
| `exec` | Выполнение системных команд |

---

## 📦 Установка

### Требования
- **Node.js** версии 18.x или выше
- **npm** версии 9.x или выше

### Шаги установки

1. **Клонируйте репозиторий:**
```bash
git clone https://github.com/yourusername/nf-void-terminal.git
cd nf-void-terminal
```

2. **Установите зависимости:**
```bash
npm install
```

3. **Запустите приложение:**
```bash
npm start
```

---

## 🚀 Сборка исполняемого файла

### Для Windows (.exe)
```bash
npm run build
```
Готовый установщик появится в папке `dist/`.

### Для других платформ
Отредактируйте `package.json` и добавьте нужную целевую платформу в секцию `"build"`.

---

## 📚 Использование

### Примеры команд

#### 🔐 Генерация безопасного пароля
```bash
passwd 32 -s
```
*Создаст пароль из 32 символов с символами*

#### 🔍 Сканирование портов
```bash
nmap localhost 1 1000
```
*Просканирует порты с 1 по 1000*

#### 📄 Аудит безопасности файла
```bash
secscan config.json
```
*Проверит файл на наличие паролей, API-ключей и других уязвимостей*

#### 🔐 Генерация хеша
```bash
hash sha256 mypassword123
```

#### 📊 Информация о системе
```bash
sysinfo
```

#### 🌐 DNS запрос
```bash
dns google.com
```

---

## 🎨 Скриншоты

<p align="center">
  <img src="Screens/NF_Void%20terminal%20v2.0%20screen_1.png" width="400" />
  <img src="Screens/NF_Void%20terminal%20v2.0%20screen_2.png" width="400" /> 
</p>
---

## 🔒 Безопасность

### Важные примечания

⚠️ **Приложение имеет реальный доступ к вашей файловой системе!**

- Команды `rm`, `mkdir`, `touch` создают/удаляют файлы на вашем компьютере
- Сканирование портов ограничено диапазоном 1-1024 для безопасности
- Системные директории защищены от удаления
- Все действия безопасности логируются в `~/.nf-void-logs/`

### Рекомендации
1. Не запускайте команды от имени администратора без необходимости
2. Проверяйте команды перед выполнением
3. Регулярно просматривайте логи безопасности

---

## 📁 Структура проекта

```
nf-void-terminal/
├── package.json          # Конфигурация проекта
├── main.js               # Главный процесс Electron
├── preload.js            # Мост безопасности
├── src/
│   ├── index.html        # HTML интерфейс
│   ├── style.css         # Стили
│   └── renderer.js       # Логика интерфейса
├── dist/                 # Собранные файлы (после build)
└── screenshots/          # Скриншоты для README
```

---

## 🤝 Вклад в проект

Мы приветствуем вклад в развитие проекта!

1. **Fork** репозиторий
2. Создайте ветку (`git checkout -b feature/AmazingFeature`)
3. Закоммитьте изменения (`git commit -m 'Add some AmazingFeature'`)
4. Push в ветку (`git push origin feature/AmazingFeature`)
5. Откройте **Pull Request**

### Правила кода
- Следуйте стилю кода в проекте
- Добавляйте комментарии к сложным функциям
- Тестируйте изменения перед отправкой

---

## 📋 Полный список команд

| Категория | Команды |
|-----------|---------|
| **Навигация** | `ls`, `cd`, `pwd` |
| **Файлы** | `cat`, `touch`, `mkdir`, `rm`, `cp`, `mv`, `echo` |
| **Система** | `clear`, `date`, `whoami`, `hostname`, `uname`, `sysinfo`, `top`, `history` |
| **Сеть** | `port`, `nmap`, `scan`, `netinfo`, `dns`, `traceroute` |
| **Безопасность** | `hash`, `passwd`, `secscan`, `audit`, `base64`, `verify`, `logs` |
| **Процессы** | `ps`, `processes`, `kill` |
| **Другое** | `exec`, `run`, `open`, `exit`, `man`, `help` |

---

## 🐛 Известные проблемы

- [ ] Сканирование портов может быть медленным на некоторых системах
- [ ] Некоторые системные команды могут отличаться на разных ОС
- [ ] Отсутствует поддержка тем оформления

---

## 📄 Лицензия

Этот проект распространяется под лицензией **MIT**. Подробнее см. в файле [LICENSE](LICENSE).

---

## 👤 Автор

- GitHub: @sboy0

---


<div align="center">

**⭐ Если вам понравился проект, поставьте звезду на GitHub! ⭐**

</div>
